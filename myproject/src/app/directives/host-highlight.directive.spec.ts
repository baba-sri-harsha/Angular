import { HostHighlightDirective } from './host-highlight.directive';

describe('HostHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new HostHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
